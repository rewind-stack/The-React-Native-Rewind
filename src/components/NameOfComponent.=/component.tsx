import React, {FC, memo} from 'react';
import {Text} from 'react-native';

import {Props, styles} from '.';

/**
 * NameOfComponent.= component description.
 *
 * @param {NameOfComponent.=Props} props - The props for the component.
 * @returns {JSX.Element} The NameOfComponent.= component.
 */
const NameOfComponent.=: FC<Props> = ({}) => {
  return <Text>NameOfComponent.=</Text>;
};

export default memo(NameOfComponent.=);
